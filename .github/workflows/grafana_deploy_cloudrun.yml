name: Grafana deploy on CloudRun

on:
  push:
    branches:
      - "production"
    paths:
      - "grafana/**"
      - ".github/workflows/grafana*"

jobs:
  build-and-push-artifact-registry:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v3
        with:
          ref: production

      -
        name: Login to GAR
        uses: docker/login-action@v2
        with:
          registry: us-central1-docker.pkg.dev
          username: _json_key
          password: ${{ secrets.GAR_JSON_KEY }}
      -
        name: Build and push (GCP)
        uses: docker/build-push-action@v3
        with:
          context: grafana/
          push: true
          tags: ${{ vars.GRAFANA_DOCKER_IMAGE }}
          
  #https://github.com/google-github-actions/deploy-cloudrun
  deploy-cloudrun:
    needs: [build-and-push-artifact-registry]
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      id-token: 'write'

    steps:
    - uses: 'actions/checkout@v3'
      with:
          ref: production
    - uses: 'google-github-actions/auth@v1'
      with:
        credentials_json: ${{ secrets.CLOUD_RUN_JSON_KEY }}

    - id: 'deploy'
      uses: 'google-github-actions/deploy-cloudrun@v1'
      with:
        service: ${{ vars.GRAFANA_SERVICE_NAME }}
        image: ${{ vars.GRAFANA_DOCKER_IMAGE }}
        env_vars: |
          PROMETHEUS_SCHEMA=${{ vars.PROMETHEUS_SCHEMA }}
          PROMETHEUS_HOST=${{ vars.PROMETHEUS_HOST }}
          PROMETHEUS_PORT=${{ vars.PROMETHEUS_PORT }}

    - name: 'Use output'
      run: 'curl "${{ steps.deploy.outputs.url }}"'
